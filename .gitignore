node_modules/
.env
out/
public/audio/
# Images are committed so they can be rendered server-side
# public/images/*.png  ← removed so DALL-E images can be pushed
# Google service account key — never commit this
credentials/*.json
# Google credentials — never commit this
drive-credentials.env
