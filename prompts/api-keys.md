# API Keys — Return of Judah Project

## OpenAI API Key (DALL-E Image Generation)

Your key starts with `sk-proj-ikbBJZD...` — do NOT commit the full key here.
Store it in your local `.env` file:

```
OPENAI_API_KEY=your-key-here
```

Or set it when running the script:
```
set OPENAI_API_KEY=your-key-here
node scripts/generate-images.js
```

**Owner:** Elder Sontonio Wiggins III  
**Use:** Generates all 9 scene photorealistic images via DALL-E 3 HD  
**Script:** `node scripts/generate-images.js`  
**Set in .env as:** `OPENAI_API_KEY=<key above>`
